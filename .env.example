# Example environment variables for agent-arborist testing
# Copy this file to .env and fill in your actual values
#
# Location: /path/to/agent-arborist/.env (repository root)
#
# HOW IT WORKS:
# 1. You put API keys here (agent-arborist/.env)
# 2. conftest.py loads this .env into the test process
# 3. git_tasks.py copies from git_root/.devcontainer/.env to worktree/.devcontainer/.env
# 4. Target's devcontainer.json uses --env-file .devcontainer/.env
# 5. Containers have API keys available for AI runners
#
# IMPORTANT: Tests run from the HOST, not inside containers
# - Arborist runs on your host machine
# - Tests create temp projects with .devcontainer/ (from fixtures)
# - For real usage: User puts .env at target-repo/.devcontainer/.env
# - git_tasks.py copies .devcontainer/.env to each worktree
# - Containers start and load the .env file from .devcontainer/.env

# Claude Code CLI (required for @pytest.mark.claude tests)
ANTHROPIC_API_KEY=sk-ant-api03-xxx
CLAUDE_CODE_OAUTH_TOKEN=your-oauth-token-here

# OpenAI API (required for @pytest.mark.opencode tests)
OPENAI_API_KEY=sk-proj-xxx

# ZAI API (optional, for opencode with alternative provider)
ZAI_API_KEY=your-zai-key-here

# Google Gemini API (required for @pytest.mark.gemini tests)
GOOGLE_API_KEY=your-google-api-key-here
